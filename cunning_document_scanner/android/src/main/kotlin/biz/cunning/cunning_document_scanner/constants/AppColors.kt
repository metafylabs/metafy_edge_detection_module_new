package biz.cunning.cunning_document_scanner.constants

import android.graphics.Color

class AppColors {
     var buttonColor : Int = Color.WHITE
     var layoutColor : Int = Color.BLACK

     companion object {    val instance = AppColors() }

}